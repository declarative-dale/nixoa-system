# SPDX-License-Identifier: Apache-2.0
# Copy this file to overrides.nix for host-local overrides that should win last.
{ ... }:
{
  # hostname = "nixoa";
  # username = "nixoa";
  # gitName = "NiXOA Admin";
  # gitEmail = "nixoa@nixoa";
  # timezone = "Europe/Paris";
  # sshKeys = [
  #   "ssh-ed25519 AAAA... user@host"
  # ];
}
